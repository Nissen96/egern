func square(arg) {
    if (arg < 5) {
        return plus(square(arg + 1), square(arg + 2));
    } else {
        return arg * arg;
    }
}

func plus(a, b) {
    return a + b;
}

var a = 4;
var b = square(a);
print(square(b));
