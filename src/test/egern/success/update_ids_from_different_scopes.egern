var main_before = 4;

func test(a, b, c, d, e, f, g, h) {
    var test_before = 3;
    func test2(i, j, k, l, m, n, o, p) {
        var test2_before = 5;
        func inner(q, r, s, t, u, v, w, x) {
            main_before = a = test_before = h = test2_before = k = o = q = w = 0;
            r = 100;
            x = 14;
            l = 10;
            p = 12;
            test2_after = 23;
            main_after = 42;
        }
        var test2_after = 2;
        inner(main_before, main_after, test_before, test_after, test2_before, test2_after, j, p);
        print(test2_before);
        print(test2_after);
    }
    var test_after = 4;
    test2(h, main_before, f, test_after, d, main_after, test_before, g);
    print(test_before);
    print(test_after);
}

var main_after = 3;

test(main_before, 1, 2, 3, 4, 5, 6, main_after);

print(main_before);
print(main_after);
