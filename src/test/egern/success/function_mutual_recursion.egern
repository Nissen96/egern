func recursiveA(n) {
    print(n);
    if (n < 1) {
        return n;
    }
    return recursiveB(n - 1);
}

func recursiveB(m) {
    print(m);
    if (m < 1) {
        return m;
    }
    return recursiveA(m - 1);
}

recursiveA(5);
