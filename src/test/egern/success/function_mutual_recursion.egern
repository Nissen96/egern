func recursiveA(n) {
    if (n < 1) {
        return n;
    }
    return recursiveB(n - 1);
}

func recursiveB(m) {
    if (m < 1) {
        return m;
    }
    return recursiveA(m - 1);
}

print(recursiveA(5));
